import getSiteConfig from '.';

describe('server', () => {
  it.todo('uses header to get site config');
  it.todo('uses query to get site config');
  it.todo('uses cookie to get site config');
  it.todo('returns 404 if unable to get site config');
  it.todo('persists cookie with the successful site');
});

describe('client', () => {
  it.todo('uses cookie to get site config');
  it.todo('returns 404 if unable to get site config');
  it.todo('persists cookie with the successful site');
});
